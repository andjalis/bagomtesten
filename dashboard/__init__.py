"""
dashboard — Streamlit dashboard package for DR Kandidattest bias analysis.

Modules:
    app        — Main Streamlit entry point (page layout skeleton)
    css        — Custom CSS styles
    data       — Cached data loading functions
    components — Shared HTML component builders
    sections/  — Chart rendering sub-package (one module per section)
"""
